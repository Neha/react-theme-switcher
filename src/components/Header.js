import React from "react";
import Nav from "./Nav";

const Header = () => {
  return (
    <header>
      <h1>Theme Switcher in ReactJS</h1>
      <Nav />
    </header>
  );
};

export default Header;
